const raio = 5.6;
// area recebe o objeto "Math" que tem a constante "PI" x A função "pow (potência)", que está dentro do objeto Math, do raio ao quadrado
const area = Math.PI * Math.pow(raio, 2);

console.log(area);
console.log(typeof Math); // "Math é um objeto"